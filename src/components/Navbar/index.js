import Button from '../Button';
import './navbar.scss';

function Navbar() {
  return (
    <div className="App">
      Home
      <Button buttonText="Sign up" type="primary" />
    </div>
  );
}

export default Navbar;
